name: Sync to GitCode

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout GitHub
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check GitCode Token
        env:
          GITCODE_TOKEN: ${{ secrets.GITCODE_TOKEN }}
        run: |
          if [ -z "${GITCODE_TOKEN}" ]; then
            echo "错误：GITCODE_TOKEN 为空"
            exit 1
          fi
          echo "GITCODE_TOKEN 已读取"

      - name: Configure GitCode Authentication
        env:
          GITCODE_TOKEN: ${{ secrets.GITCODE_TOKEN }}
        run: |
          cat > ~/.netrc <<EOF
          machine gitcode.com
          login gcw_7DpHPfUr
          password ${GITCODE_TOKEN}
          EOF

          chmod 600 ~/.netrc

      - name: Test GitCode Authentication
        run: |
          git ls-remote https://gitcode.com/gcw_7DpHPfUr/soa-userscripts.git

      - name: Push to GitCode
        run: |
          git remote remove gitcode 2>/dev/null || true

          git remote add gitcode \
            https://gitcode.com/gcw_7DpHPfUr/soa-userscripts.git

          git push gitcode HEAD:main
          git push gitcode --tags
